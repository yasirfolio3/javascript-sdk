{
  "name": "event-tracking",
  "version": "1.0.0",
  "description": "Event tracking example for Optimizely javascript sdk",
  "main": "index.js",
  "scripts": {
    "compile": "mkdir -p dist && sed \"s/{{OPTY_SDK_KEY}}/$OPTY_SDK_KEY/g\" index.html > dist/index.html",
    "start": "npm run compile && serve ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Optimizely",
  "license": "ISC",
  "devDependencies": {
    "serve": "^11.3.0"
  }
}
